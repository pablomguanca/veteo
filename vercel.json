{
    "version": 2,
    "name": "veteo",
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "functions": {
        "api/cargar-catalogo.js": {
            "maxDuration": 60,
            "memory": 1024
        }
    },
    "routes": [
        {
            "src": "/sw.js",
            "headers": {
                "Cache-Control": "no-cache, no-store, must-revalidate",
                "Service-Worker-Allowed": "/"
            },
            "dest": "/sw.js"
        },
        {
            "src": "/(.*)",
            "dest": "/$1"
        }
    ]
}